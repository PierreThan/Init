# Trouvez une commande qui permet, en temps réel, de trouver les processus en
# cours d’exécution

# INFO:
# The top program provides a dynamic real-time view of a running system.
# It can display system summary information as well as a list of processes
# or threads currently being managed by the Linux kernel.
# The types of system summary information shown and the types, order and size
# of information displayed for processes are all user configurable and that
# configuration can be made persistent across restarts.

# COMMAND:
top
